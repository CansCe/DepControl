/// What this collector calls itself in the bundles it writes.
///
/// Travels with every bundle so a report can name the tool that produced it —
/// the one piece of provenance a server has when a bundle reads oddly, since
/// everything else about the machine that made it is deliberately absent.
const collectorVersion = '0.1.0';
