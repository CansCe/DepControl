name: backend
description: DepControl API (Dart Frog).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  dart_frog: ^1.1.0
  # Resolves to the local workspace member `packages/shared`.
  shared: ^0.1.0
  pubspec_parse: ^1.3.0
  pub_semver: ^2.1.4
  yaml: ^3.1.2
  http: ^1.2.0
  uuid: ^4.4.0

dev_dependencies:
  dart_frog_cli: ^1.1.0
  lints: ^6.0.0
  test: ^1.25.0
